# Sourced (set -a) before pytest runs in the `tests` and `diff-coverage` CI
# gates. Put non-secret test configuration here — e.g. SESSION_SECRET_KEY for
# apps whose settings require one in CI. Never real secrets: this file is
# committed.
EXAMPLE_GREETING_SUBJECT=colormath
