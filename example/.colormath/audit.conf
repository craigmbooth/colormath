# Sourced by colormath's scripts/audit-deps.sh (the `deps` CI gate).
# See that script's header for the full contract.

# Poetry groups that ship to production, passed to `poetry export --with`.
# Empty = main group only. (Example app ships only its main deps.)
POETRY_EXPORT_GROUPS=""

# Space-separated CVE/PYSEC ids to ignore. Every entry needs a justification
# comment + a revisit trigger. Keep this list as small as possible.
IGNORE_VULNS=""
